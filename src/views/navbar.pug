
nav(class="navbar")
    ul(class="nav-elements")
        li(class="nav-link") 
            a(href="/") Home 
        li(class="nav-link")
            a(href="/cart") Carrito
        li(class="nav-link")    
            a(href="/vinyl") Vinilos 
        if !session.user
            li(class="nav-link")    
                a(href="/user/register") Registro        
            li(class="nav-link")    
                a(href="/user/login") Login     
        else 
            li(class="nav-link")
                form(class="nav-link" action="/user/logout", method="post")     
                    button  Logout
            p(class="nav-link")=("Hola: "+session.user.user_name)
            if session.user.user_rol=="admin"
                li(class="nav-link")    
                    a(href="/genre") Generos
                li(class="nav-link")    
                    a(href="/user") Usuarios
